[pytest]
testpaths = tests
asyncio_mode = auto
# test_coral.py / test_sentry.py are manual scripts that need live Coral
# credentials and a running Coral binary; they are not unit tests.
addopts = --ignore=tests/test_coral.py --ignore=tests/test_sentry.py
